<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="krelve.app.Easy">


    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />


    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.write_external_storage"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name="krelve.app.Easy.Kpplication"
        android:allowBackup="true"
        android:icon="@mipmap/ok"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name="krelve.app.Easy.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.Register" />


        <activity
            android:name="krelve.app.Easy.activity.MainActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name="krelve.app.Easy.activity.LatestContentActivity"
            android:theme="@style/AppTheme.TransparentActivity"></activity>
        <activity
            android:name="krelve.app.Easy.activity.NewsContentActivity"
            android:theme="@style/AppTheme.TransparentActivity"></activity>
        <activity android:name=".activity.ChooseActivity"></activity>

        <activity android:name=".activity.ImageDisplay"/>
        <activity android:name=".activity.ReadBlog" />
        <activity android:name=".activity.SendActivity" />
        <activity android:name=".activity.SchoolInfo"/>
        <activity android:name=".activity.GridViewActivity"/>
        <activity android:name=".activity.LoginActivity"/>

        <activity android:name=".activity.CropActivity"/>
        <service android:name=".service.MyService">


        </service>

        <service android:name=".service.PushService"></service>

    </application>


</manifest>
